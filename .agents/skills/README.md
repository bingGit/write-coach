# Agent Skills 使用指南

## 📚 什么是 Agent Skills？

Agent Skills 是一套可以让 AI 助手（如 Antigravity）自动遵循的最佳实践规则集。就像给 AI 配备了一本"专业手册"，让它在编写代码时自动应用业界最佳实践。

## 🎯 为什么需要它？

### 问题场景
在开发 React/Next.js 应用时，你可能会遇到：

1. **性能问题**
   - 页面加载慢
   - 打包体积过大
   - 组件频繁重渲染
   - 数据请求瀑布流（waterfall）

2. **代码质量问题**
   - 不知道如何优化异步操作
   - 不确定何时使用 `useMemo`、`useCallback`
   - 不清楚服务端和客户端组件的最佳实践
   - 导入方式导致打包体积增大

3. **开发效率问题**
   - 每次都要查文档找最佳实践
   - 代码审查时发现性能问题
   - 重构时不确定优化方向

### 解决方案

安装 `vercel-react-best-practices` 技能后，AI 助手会：

✅ **自动应用 45 条性能优化规则**  
✅ **在编写代码时主动建议最佳实践**  
✅ **在代码审查时指出潜在性能问题**  
✅ **提供具体的优化建议和代码示例**

## 📦 已安装的技能

### vercel-react-best-practices

来自 Vercel 工程团队的官方性能优化指南，包含 **45 条规则**，分为 8 个优先级类别：

| 优先级 | 类别 | 影响程度 | 规则数量 |
|--------|------|----------|----------|
| 🔴 关键 | 消除瀑布流 | CRITICAL | 5 条 |
| 🔴 关键 | 包体积优化 | CRITICAL | 5 条 |
| 🟠 高 | 服务端性能 | HIGH | 5 条 |
| 🟡 中高 | 客户端数据获取 | MEDIUM-HIGH | 2 条 |
| 🟡 中 | 重渲染优化 | MEDIUM | 7 条 |
| 🟡 中 | 渲染性能 | MEDIUM | 7 条 |
| 🟢 中低 | JavaScript 性能 | LOW-MEDIUM | 12 条 |
| 🟢 低 | 高级模式 | LOW | 2 条 |

## 🚀 使用场景

### 1. 编写新代码时

**场景：** 你正在创建一个新的 React 组件

```typescript
// ❌ AI 会提醒你避免这样写
import { Button, Icon, Modal } from '@/components'; // barrel import 问题

function MyComponent() {
  const [data, setData] = useState(fetchData()); // 昂贵的初始化
  // ...
}
```

**AI 会建议：**

```typescript
// ✅ 直接导入，避免 barrel files
import { Button } from '@/components/Button';
import { Icon } from '@/components/Icon';
import { Modal } from '@/components/Modal';

function MyComponent() {
  // 使用函数形式的 lazy initialization
  const [data, setData] = useState(() => fetchData());
  // ...
}
```

### 2. 代码审查时

**你可以问：**
> "帮我审查这个组件的性能问题"

**AI 会检查：**
- ✅ 是否有不必要的重渲染
- ✅ 异步操作是否并行执行
- ✅ 是否正确使用了缓存
- ✅ 包体积是否可以优化
- ✅ 是否有瀑布流请求

### 3. 性能优化时

**场景：** 页面加载慢，需要优化

**你可以说：**
> "这个页面加载很慢，帮我优化一下"

**AI 会：**
1. 分析数据获取逻辑，建议并行化
2. 检查组件导入，建议动态导入
3. 审查重渲染问题，建议使用 memo
4. 优化打包体积，延迟加载第三方库

### 4. 重构代码时

**场景：** 重构旧代码以提升性能

**你可以说：**
> "帮我重构这个组件，应用最佳实践"

**AI 会应用：**
- 服务端组件优先
- 正确的缓存策略
- 优化的状态管理
- 减少客户端 JavaScript

## 💡 实际使用示例

### 示例 1：优化数据获取

**你的代码：**
```typescript
async function getData() {
  const user = await fetchUser();
  const posts = await fetchPosts();
  const comments = await fetchComments();
  return { user, posts, comments };
}
```

**AI 建议：**
```typescript
// 使用 Promise.all 并行获取独立数据
async function getData() {
  const [user, posts, comments] = await Promise.all([
    fetchUser(),
    fetchPosts(),
    fetchComments(),
  ]);
  return { user, posts, comments };
}
```

**规则：** `async-parallel` - 对独立操作使用 Promise.all()

---

### 示例 2：优化包体积

**你的代码：**
```typescript
import { Chart } from 'chart.js';

function Dashboard() {
  return <Chart data={data} />;
}
```

**AI 建议：**
```typescript
import dynamic from 'next/dynamic';

// 动态导入重型组件
const Chart = dynamic(() => import('chart.js').then(mod => mod.Chart), {
  loading: () => <p>加载中...</p>,
  ssr: false
});

function Dashboard() {
  return <Chart data={data} />;
}
```

**规则：** `bundle-dynamic-imports` - 对重型组件使用动态导入

---

### 示例 3：优化重渲染

**你的代码：**
```typescript
function Parent() {
  const [count, setCount] = useState(0);
  
  return (
    <div>
      <ExpensiveChild onUpdate={() => setCount(count + 1)} />
    </div>
  );
}
```

**AI 建议：**
```typescript
function Parent() {
  const [count, setCount] = useState(0);
  
  // 使用函数式 setState，回调函数引用稳定
  const handleUpdate = useCallback(() => {
    setCount(c => c + 1);
  }, []);
  
  return (
    <div>
      <ExpensiveChild onUpdate={handleUpdate} />
    </div>
  );
}
```

**规则：** `rerender-functional-setstate` - 使用函数式 setState 保持回调稳定

## 🎓 如何触发技能

技能会在以下情况**自动激活**：

### 关键词触发
- "审查这个 React 组件"
- "优化性能"
- "帮我重构"
- "为什么这个组件这么慢"
- "减小打包体积"
- "优化加载速度"

### 代码上下文触发
当你打开 React/Next.js 文件时，AI 会自动应用相关规则：
- `.tsx`、`.jsx` 文件
- Next.js 的 `app/` 或 `pages/` 目录
- 包含 React hooks 的代码
- 数据获取逻辑

### 主动请求
你也可以明确要求：
> "用 Vercel 最佳实践审查这段代码"

## 📖 深入学习

### 查看完整规则

所有规则的详细说明都在这里：

```bash
# 查看规则概览
cat .agents/skills/vercel-react-best-practices/SKILL.md

# 查看完整文档（包含所有示例）
cat .agents/skills/vercel-react-best-practices/AGENTS.md

# 查看具体规则（例如：并行异步）
cat .agents/skills/vercel-react-best-practices/rules/async-parallel.md
```

### 规则文件结构

每个规则文件包含：
- ❌ **错误示例** - 为什么这样不好
- ✅ **正确示例** - 应该怎么写
- 📝 **详细解释** - 背后的原理
- 🔗 **参考链接** - 更多资源

## 🔧 管理技能

### 更新技能

```bash
# 拉取最新版本
cd .agents/skills/vercel-react-best-practices
git pull
```

### 添加更多技能

```bash
# 从 Vercel Labs 安装其他技能
npx add-skill vercel-labs/agent-skills

# 或从其他来源安装
npx add-skill <github-repo>
```

### 禁用技能

如果你想临时禁用某个技能：

```bash
# 重命名技能目录
mv .agent/skills/vercel-react-best-practices .agent/skills/_vercel-react-best-practices.disabled
```

## 🎯 最佳实践建议

### 1. 信任但验证
- AI 的建议基于最佳实践，但要理解**为什么**这样做
- 对于不理解的建议，要求 AI 解释原理

### 2. 渐进式优化
- 不要一次性重构所有代码
- 优先处理**关键路径**的性能问题
- 从 CRITICAL 优先级的规则开始

### 3. 结合实际测量
- 使用 Chrome DevTools 验证优化效果
- 用 Lighthouse 测量性能分数
- 监控实际的 Core Web Vitals

### 4. 团队协作
- 将技能规则作为团队代码规范的一部分
- 在 Code Review 时引用具体规则
- 定期分享学到的优化技巧

## 📊 预期收益

应用这些最佳实践后，你可以期待：

- 🚀 **加载速度提升 30-50%**
- 📦 **打包体积减少 20-40%**
- ⚡ **首屏渲染时间减少 40-60%**
- 🎯 **Lighthouse 分数提升到 90+**
- 💚 **更好的用户体验和 SEO 排名**

## 🤝 贡献

如果你发现规则有问题或想添加新规则：

1. 访问 [vercel-labs/agent-skills](https://github.com/vercel-labs/agent-skills)
2. 提交 Issue 或 Pull Request
3. 与社区分享你的优化经验

## 📚 相关资源

- [Next.js 性能优化文档](https://nextjs.org/docs/app/building-your-application/optimizing)
- [React 性能优化指南](https://react.dev/learn/render-and-commit)
- [Vercel 博客 - 性能最佳实践](https://vercel.com/blog)
- [Web.dev - Core Web Vitals](https://web.dev/vitals/)

---

**记住：** 这些技能是你的 AI 助手的"专业知识库"，它会在你需要时自动提供帮助。继续编码，让 AI 帮你写出更快、更优的代码！🚀
